{$TRIAL_HOST} {
  @outside not remote_ip {$TRIAL_ALLOWED_CIDRS}
  respond @outside 403
  header X-Robots-Tag "noindex, nofollow, noarchive"
  reverse_proxy app:3000 {
    header_up -Forwarded
    header_up -X-Forwarded-For
    header_up X-Real-IP {remote_host}
  }
}
