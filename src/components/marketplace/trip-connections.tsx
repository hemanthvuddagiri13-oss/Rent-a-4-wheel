import Link from "next/link";
import { ActionForm } from "./action-form";
export function TripConnections({reservationId}:{reservationId:string}) { return <section className="my-6 space-y-4 rounded-xl border border-white/10 p-5"><h2 className="text-xl font-semibold">Messages & support</h2><ActionForm endpoint="/api/community" action="conversation" values={{reservationId}} label="Message trip participants" redirectTo="/connect/conversations/:id"/><div className="flex flex-wrap gap-4 text-gold"><Link href={`/connect?view=cases&reservationId=${reservationId}`}>Open a claim or dispute</Link><Link href={`/connect?view=support&reservationId=${reservationId}`}>Roadside & support</Link><Link href={`/connect?view=reviews&reservationId=${reservationId}`}>Review this trip</Link></div></section>; }
